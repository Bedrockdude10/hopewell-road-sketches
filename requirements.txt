





#
#
#
#     its linter is missing, which is the whole point (see that file's docstring). ---
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# --- 3D export ---
# --- Config and I/O ---
# --- Geometry and geodata: the actual subject ---
# --- Plotting (the 2D plan view) ---
# --- Test tooling. Required, not optional: tests/test_lint.py FAILS rather than skips when
# --- Transitive, pinned so an install reproduces ---
# against undefined names reported success on every machine that had not installed it by hand.
# because a second file you have to remember is the same bug this repo has already shipped once:
# Everything this repo needs, in one file, installed by one command:
# Note the Blender scripts (scripts/blender/) run inside Blender's OWN interpreter and use none
# of this - see their module docstrings.
# ONE FILE ON PURPOSE. The test tooling at the bottom is not split into a requirements-dev.txt,
# pyflakes was what tests/test_lint.py ran and it was in nobody's requirements, so the guard
# pytest was missing here too, which means the documented install has never actually been able
# split would buy nothing and cost exactly that.
# test one: src/site.py imports it, so every phase script does.
# to run the suite. There is no packaging step and nothing here is deployed anywhere slim, so a
# Validates every site's config.yaml on load (src/site_schema.py). A RUNTIME dependency, not a
annotated-types==0.8.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.2
contourpy==1.3.3
cycler==0.12.1
fast_simplification==0.1.13
fonttools==4.63.0
geographiclib==2.1
geopandas==1.1.4
geopy==2.4.1
grimp==3.15
hypothesis==6.165.5
idna==3.18
import-linter==2.13
iniconfig==2.3.0
kiwisolver==1.5.0
mapbox_earcut==2.0.0
markdown-it-py==4.2.0
matplotlib==3.11.0
mdurl==0.1.2
numpy==2.5.0
packaging==26.2
pandas==3.0.3
pandera==0.32.1
pillow==12.3.0
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
pyogrio==0.13.0
pyparsing==3.3.2
pyproj==3.7.2
pytest-datadir==1.8.0
pytest-randomly==4.1.0
pytest-regressions==2.11.0
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
requests==2.34.2
rich==15.0.0
ruff==0.16.3
shapely==2.1.2
six==1.17.0
sortedcontainers==2.4.0
trimesh==4.12.2
typeguard==4.6.0
typing-inspect==0.9.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
